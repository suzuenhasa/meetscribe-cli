__pycache__/
*.pyc

# Everything setup.sh creates lives inside this checkout.
venv/
.hf_home/
wsp_ckpt/
wespeaker_src/
env.sh
speakers.db
inbox/
out/
runs/
_meetscribe_src/

# transcripts land beside your audio, not in here
*.txt
!glossary.txt.example
*.json
*.npz

# A library is someone's meetings and their audio. It is created at runtime,
# next to the checkout by default, and must never be committed -- the uploads
# under _inbox/ and the transcripts beside them are exactly the private data
# this tool exists to keep on your own machine.
library/

# Working notes and architecture maps -- not part of the tool.
plans/
